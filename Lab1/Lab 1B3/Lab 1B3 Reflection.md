# Lab Questions and Answers
1. How many times does "verdigris" appear?

   Answer: 0
   

3. What is the surname of the author of 11.07.txt?

   Answer: Cannot be answered because the 1107.txt was not found in the extracted files.
   

5. What is the surname of the author of the file that is exactly 255258 bytes?

   Answer: Cannot be answered because no extracted file matched that size.
   

7. What is the name of the file with the 3rd oldest date?

   Answer: frankensteint.txt
   

9. Find the word that follows the sentence about Jack?

   Answer: Cannot be answered because the sentence was not found in the extracted files.


# Lab 1B3 Reflection

- In this lab, I practised extracting a compressed archive using `bunzip2` and `tar`. After extracting it, I checked what text files were inside the folder.

- I used `find` to search for files and `grep` to search for words or sentences inside the text files. I found these two commands the most useful because they helped me check the files faster without opening each one manually.

- I also used commands like `du`, `sed`, `sort`, and `uniq` to check file sizes and word frequency. This showed me that Linux commands can be joined together to analyse files more quickly.

- One thing I noticed was that the archive only had three text files: `moby.txt`, `frankenstein.txt`, and `twocities.txt`. Because of this, some expected files and text were not found, but I still used the commands to show the search results.

- I think these search commands are useful in cybersecurity because they can help check logs, files, or suspicious text quickly.

- This lab also made me realise that scripting would be helpful for repeated searches, because the commands can be saved and run again instead of typing them one by one.
